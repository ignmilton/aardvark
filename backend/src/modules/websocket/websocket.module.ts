import { Module } from "@nestjs/common";

// TODO: Implement WebSocket gateway for real-time notifications and live updates.
// The IoAdapter is configured in main.ts; this module needs a gateway class
// decorated with @WebSocketGateway() to handle socket connections.
@Module({})
export class WebsocketModule {}
